# Vesper — Environment Configuration
# API keys and trading config are now managed per-user via the web dashboard.
# This file only contains server-level settings.

# Data directory for SQLite DB, portfolios, and logs
VESPER_DATA_DIR=data
