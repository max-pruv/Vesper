# Vesper — Environment Configuration
# API keys and trading config are managed per-user via the web dashboard.

# Data directory
VESPER_DATA_DIR=data

# Domain for HTTPS (used by Caddy for auto-SSL)
DOMAIN=srv1438426.hstgr.cloud

# Google OAuth (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Apple OAuth (https://developer.apple.com/account/resources/identifiers)
APPLE_CLIENT_ID=
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY=
